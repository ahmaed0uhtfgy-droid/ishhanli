{
  "name": "ishhanli",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.5" },
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=ishhanli",
    "db:create": "wrangler d1 create ishhanli-db && wrangler d1 create ishhanli-files",
    "db:init:local": "wrangler d1 execute ishhanli-db --local --file=schema.sql && wrangler d1 execute ishhanli-files --local --file=schema-files.sql",
    "db:init:remote": "wrangler d1 execute ishhanli-db --remote --file=schema.sql && wrangler d1 execute ishhanli-files --remote --file=schema-files.sql",
    "test": "node --no-warnings test/server.test.mjs && node --no-warnings test/client.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
