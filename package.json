{
  "name": "ishhanli",
  "version": "0.4.0-pages",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=ishhanli-app"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
